# Example: Jose (HS256) JWT + next-route-guard

Set `adapter: "jose"`, `secret`, and optionally `cookieName`. Issue JWTs signed with the same secret (e.g. from your login route).

See the main `README.md` for a full quick start.
